apiVersion: v1
kind: Secret
metadata:
  # Replace RELEASE_NAME and NAMESPACE to match your Helm install
  name: RELEASE_NAME-datahub-backend
  namespace: datahub
type: Opaque
stringData:
  APP_SECRET_KEY: "REPLACE_ME"
  OIDC_CLIENT_SECRET: "REPLACE_ME"
  SHARE_SIGNING_SECRET: "REPLACE_ME"
  DATABASE_URL: "REPLACE_ME"
  REDIS_URL: "REPLACE_ME"
  CHROMA_URL: "REPLACE_ME"
  SUPABASE_ANON_KEY: "REPLACE_ME"
  SUPABASE_SERVICE_ROLE_KEY: "REPLACE_ME"
